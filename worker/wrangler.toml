name = "phantom-node-worker"
main = "src/index.ts"
compatibility_date = "2024-11-06"

# Local dev defaults. Run with: npm run dev (inside /worker)
# Default URL: http://127.0.0.1:8787
[dev]
port = 8787
local_protocol = "http"

[observability]
enabled = true
