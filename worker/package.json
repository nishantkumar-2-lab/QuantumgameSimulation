{
  "name": "phantom-node-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker quantum execution backend for Phantom Node (BB84 simulator).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.86.0"
  }
}
